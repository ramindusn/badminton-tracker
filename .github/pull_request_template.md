## What & why
<!-- What does this PR change, and why? Keep it short. -->

## Checklist
- [ ] Verification gate is green locally: `npm run lint && npm run test && npm run test:e2e`
- [ ] No secrets committed — `.env.local` stays local; only `VITE_`-safe values in client code
- [ ] Updated `AGENTS.md` if a convention or architectural decision changed

<!--
CI (Lint & unit tests / End-to-end tests / Docker image build) must pass and
@ramindusn must approve before this can merge to main.
-->
